# 🚀 GitHub Setup Commands
# Run these commands after creating your GitHub repository

# STEP 1: Create the repository on GitHub
# Go to: https://github.com/new
# Repository name: SayCheese (or your preferred name)
# Description: YouTube Channel Analytics Dashboard with 90 Indian channels
# Public or Private: Your choice
# Do NOT check "Initialize with README"

# STEP 2: Copy your repository URL from GitHub
# It will look like: https://github.com/YOUR_USERNAME/SayCheese.git

# STEP 3: Run these commands (replace YOUR_USERNAME with your actual GitHub username)

# Add the remote repository
git remote add origin https://github.com/YOUR_USERNAME/SayCheese.git

# Verify the remote was added
git remote -v

# Push your code to GitHub
git push -u origin master

# OR if your default branch is 'main':
# git branch -M main
# git push -u origin main

# ✅ That's it! Your project will be on GitHub!

# 📝 For future updates, use:
# git add .
# git commit -m "Your commit message"
# git push
